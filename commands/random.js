module.exports = {
	name: 'random',
	description: 'random thing if you want',
	execute(message) {
		// var item = items[Math.floor(Math.random() * items.length)];
		message.channel.send("dedicate feature");
	},
};
